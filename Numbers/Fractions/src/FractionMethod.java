/**
 * Created by yaweili on 3/8/17.
 */
public interface FractionMethod {

    Fraction add(Fraction fraction);

    Fraction subtract(Fraction fraction);

    Fraction multiply(Fraction fraction);

    Fraction divide(Fraction fraction);
}
